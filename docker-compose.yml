version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "4242:4242"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./screenshots:/app/screenshots
    restart: unless-stopped
